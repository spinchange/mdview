[package]
name = "settings-store"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
win-theme-watcher = { path = "../win-theme-watcher" }
